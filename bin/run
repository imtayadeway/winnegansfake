#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.expand_path(File.join(__dir__, "..", "lib")))
require "winnegansfake"

loop do
  if WinnegansFake.post?
    WinnegansFake.post
  else
    sleep(60)
  end
end
