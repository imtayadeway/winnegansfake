#!/usr/bin/env ruby

require "bundler/setup"
require "sequel"

DB = Sequel.postgres
DB.execute("CREATE DATABASE winnegansfake")

DB.create_table? :cursor do
  primary_key :id
  Integer :value, null: false
end

DB.create_table? :timestamp do
  primary_key :id
  DateTime :value, null: false
end
