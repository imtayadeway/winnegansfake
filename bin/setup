#!/usr/bin/env ruby

require "bundler/setup"
require "sequel"

DB = Sequel.connect(ENV.fetch("DATABASE_URL"))

DB.create_table? :cursor do
  primary_key :id
  Integer :value, null: false
end

DB.create_table? :timestamp do
  primary_key :id
  DateTime :value, null: false
end
